# Allowlisted GO vuln IDs only (one ID per line; # comments and blanks ignored).
# Prefer fixing reachable findings (toolchain / dependency bumps). Add an ID here
# only when there is no module-path fix and the residual is documented in
# docs/security-defaults.md.
#
# Nested compute uses github.com/moby/moby/client + github.com/moby/moby/api
# (not github.com/docker/docker). Fixed-N/A Docker Engine IDs that govulncheck
# attributed to the legacy client module path no longer apply. Keep empty
# unless a new residual with no module-path fix appears.
